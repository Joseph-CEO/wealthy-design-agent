version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./designer_agent.db:/app/designer_agent.db
    restart: unless-stopped

  # Add a PostgreSQL service for production:
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: designer_agent
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   restart: unless-stopped
  #
  # Then set DATABASE_URL=postgresql+asyncpg://postgres:${DB_PASSWORD}@db:5432/designer_agent
  # and add `asyncpg` to requirements.txt

# volumes:
#   pgdata:
