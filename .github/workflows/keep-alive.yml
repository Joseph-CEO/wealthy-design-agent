name: Keep Railway Backend Awake
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: "curl -sS -o /dev/null -w '%{http_code}' https://api-production-8de3.up.railway.app/api/v1/health"
